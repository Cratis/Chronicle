import { TopSection } from './TopSection';
import { Menu } from '../Menu/Menu';

export const Sidebar = () => {
    return (
        <div>
            <TopSection />
            <Menu />
        </div>
    );
};
