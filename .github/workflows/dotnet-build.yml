name: .NET Build & Integration

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  DOTNET8_VERSION: "8.0.410"
  DOTNET9_VERSION: "9.0.x"
  DOTNET_VERSION: "10.0.x"
  DOTNET_CACHE: "dotnet-cache-${{ github.sha }}"
  # Separate cache for the DEVELOPMENT-symbol build the integration tests consume.
  DOTNET_DEV_CACHE: "dotnet-dev-cache-${{ github.sha }}"
  CRATIS_CHRONICLE_LOCAL_IMAGE: "ghcr.io/cratis/chronicle:${{ github.sha }}"

on:
  workflow_dispatch:
  # Nightly full-coverage run. Pull requests cover mongodb and sqlite, so this is
  # where postgresql and mssql get exercised.
  schedule:
    - cron: "0 2 * * *"
  pull_request:
    branches:
      - "**"
    paths:
      - "Source/**/*.cs"
      - "Source/**/*.csproj"
      # Central package management decides what every project in the repository compiles against, so a version
      # bump here reaches further than any single project file - and reached it without building, because a
      # change confined to these files matched none of the patterns above. Directory.Build.props for the same
      # reason: it sets the properties the build runs under.
      - "Directory.Packages.props"
      - "Directory.Build.props"
      # A project added to or dropped from the solution changes what gets built and what gets tested,
      # without necessarily touching a single source file. The membership assertion below reads it.
      - "Chronicle.slnx"
      - "Integration/**"
      # The benchmarks job builds these, so a change here has to run it. Without this a broken benchmark
      # project lands green and fails on the next unrelated pull request instead.
      - "Benchmarks/**/*.cs"
      - "Benchmarks/**/*.csproj"
      - ".github/workflows/dotnet-build.yml"
      - ".github/workflows/integration.yml"
      # The generator decides which namespaces, shards and storage backends the
      # integration stage runs, so a change to it changes what this workflow
      # verifies - and without this pattern it would change it without ever
      # running. Its specs come along for the same reason.
      - ".github/scripts/generate-integration-matrix.py"
      - ".github/scripts/test-generate-integration-matrix.py"

jobs:
  dotnet-build:
    runs-on: ubuntu-latest
    outputs:
      dotnet-cache-key: ${{ steps.export-cache-key.outputs.key }}
      chronicle-image: ${{ steps.export-cache-key.outputs.image }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Runs before anything is compiled because it needs nothing but the checkout, and because a spec
      # project missing from the solution is invisible to every later step: it is never built, never
      # invoked, and therefore writes no TRX for the zero-test guard to find.
      - name: Assert every spec project is in the solution
        run: python3 .github/scripts/assert-solution-membership.py

      - name: Setup .Net
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            ${{ env.DOTNET8_VERSION }}
            ${{ env.DOTNET9_VERSION }}
            ${{ env.DOTNET_VERSION }}

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-${{ runner.os }}-${{ hashFiles('**/*.csproj', '**/Directory.*.props', '**/Directory.*.targets') }}
          restore-keys: |
            nuget-${{ runner.os }}-

      - name: Restore obj cache
        uses: actions/cache@v4
        with:
          path: ./**/obj
          # Key on the package manifests so a dependency version change never restores a stale obj
          # cache from a different package set. A loose restore-key previously reused a pre-bump obj
          # cache across a package bump, producing a skewed publish where an older Cratis.Arc.Core.dll
          # shipped alongside newer Arc assemblies that referenced types it no longer contained.
          key: dotnet-obj-cache-${{ hashFiles('**/*.csproj', '**/Directory.*.props', '**/Directory.*.targets') }}-${{ github.sha }}
          restore-keys: |
            dotnet-obj-cache-${{ hashFiles('**/*.csproj', '**/Directory.*.props', '**/Directory.*.targets') }}-

      - uses: actions/cache@v4
        id: dotnet-x64-output
        with:
          path: ./**/bin
          key: ${{ env.DOTNET_CACHE }}

      - name: Login to GitHub Docker Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Add GitHub Package Registry to NuGet
        run: |
          dotnet nuget update source github --username cratis --password ${{ secrets.GITHUB_TOKEN }} --store-password-in-clear-text || \
          dotnet nuget add source --name github --username cratis --password ${{ secrets.GITHUB_TOKEN }} --store-password-in-clear-text https://nuget.pkg.github.com/cratis/index.json

      - name: Build
        run: dotnet build --configuration Release -p:DisableProxyGenerator=true -p:DisableDockerBuild=true

      # The build above is net10.0 only. The client packages ship net8.0 and net9.0 as well, and that multi-target
      # happens only under IsPackaging - which until now nothing ran before publish. So an API newer than net8.0
      # passed every check on a pull request and failed while the release was being cut, with the merge already
      # done. This builds what publish packs, on the frameworks it packs them for. The SDKs are already installed
      # above for exactly this reason.
      - name: Build client packages for every shipped framework
        run: |
          set -e
          for project in $(find ./Source/Clients -name "*.csproj" ! -name "*.Specs.csproj"); do
            dotnet build "$project" --configuration Release -p:IsPackaging=true -p:DisableProxyGenerator=true -p:DisableDockerBuild=true
          done
          dotnet build ./Source/Kernel/Contracts/Contracts.csproj --configuration Release -p:IsPackaging=true -p:DisableProxyGenerator=true -p:DisableDockerBuild=true

      - name: Build and push development Docker image
        run: |
          dotnet publish Source/Kernel/Server/Server.csproj \
            --configuration Debug \
            --no-restore \
            -o Source/Kernel/Server/out
          docker build -t "${{ env.CRATIS_CHRONICLE_LOCAL_IMAGE }}" -f Docker/Local/Dockerfile .
          docker push ${{ env.CRATIS_CHRONICLE_LOCAL_IMAGE }}

      - name: Export cache key and image
        id: export-cache-key
        run: |
          echo "key=${{ env.DOTNET_CACHE }}" >> "$GITHUB_OUTPUT"
          echo "image=${{ env.CRATIS_CHRONICLE_LOCAL_IMAGE }}" >> "$GITHUB_OUTPUT"

  # The integration tests run with the DEVELOPMENT compile symbol, which the
  # Release build above does not define, so every integration shard would
  # otherwise recompile ~15-20 projects from source before its first test.
  # Build that variant once here -- in parallel with dotnet-build, so it does
  # not extend the critical path -- and cache it under its own key for the
  # shards to restore and run with --no-build. The non-DEVELOPMENT cache stays
  # for the specs/mongodb/integration-api jobs, which must exercise production
  # (non-DEVELOPMENT) behavior.
  dotnet-build-development:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    outputs:
      dotnet-cache-key: ${{ steps.export-dev-cache-key.outputs.key }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .Net
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            ${{ env.DOTNET8_VERSION }}
            ${{ env.DOTNET9_VERSION }}
            ${{ env.DOTNET_VERSION }}

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-${{ runner.os }}-${{ hashFiles('**/*.csproj', '**/Directory.*.props', '**/Directory.*.targets') }}
          restore-keys: |
            nuget-${{ runner.os }}-

      - name: Restore obj cache
        uses: actions/cache@v4
        with:
          path: ./**/obj
          # Package-aware key (see the dotnet-build job) so a dependency bump busts the stale obj cache.
          key: dotnet-dev-obj-cache-${{ hashFiles('**/*.csproj', '**/Directory.*.props', '**/Directory.*.targets') }}-${{ github.sha }}
          restore-keys: |
            dotnet-dev-obj-cache-${{ hashFiles('**/*.csproj', '**/Directory.*.props', '**/Directory.*.targets') }}-

      - uses: actions/cache@v4
        id: dotnet-dev-output
        with:
          path: ./**/bin
          key: ${{ env.DOTNET_DEV_CACHE }}

      - name: Add GitHub Package Registry to NuGet
        run: |
          dotnet nuget update source github --username cratis --password ${{ secrets.GITHUB_TOKEN }} --store-password-in-clear-text || \
          dotnet nuget add source --name github --username cratis --password ${{ secrets.GITHUB_TOKEN }} --store-password-in-clear-text https://nuget.pkg.github.com/cratis/index.json

      - name: Build with DEVELOPMENT symbol
        run: dotnet build --configuration Release -p:EnableDevelopmentSymbol=true -p:DisableProxyGenerator=true -p:DisableDockerBuild=true

      - name: Export dev cache key
        id: export-dev-cache-key
        run: echo "key=${{ env.DOTNET_DEV_CACHE }}" >> "$GITHUB_OUTPUT"

  specs:
    runs-on: ubuntu-latest
    needs: [dotnet-build]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .Net
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            ${{ env.DOTNET8_VERSION }}
            ${{ env.DOTNET9_VERSION }}
            ${{ env.DOTNET_VERSION }}

      - uses: actions/cache@v4
        id: dotnet-x64-output
        with:
          path: ./**/bin
          key: ${{ env.DOTNET_CACHE }}

      - name: Run tests with coverage
        # Integration tests are executed by .github/workflows/integration.yml.
        # Keep regular specs focused on non-integration test suites.
        run: dotnet test --no-build --configuration Release --settings specs.runsettings --filter "FullyQualifiedName!~Cratis.Chronicle.Integration&FullyQualifiedName!~Cratis.Chronicle.Kernel.Integration" --collect:"XPlat Code Coverage" --results-directory ./coverage-results --logger "trx;LogFileName=results.trx"

      - name: Assert specs ran
        uses: ./.github/actions/assert-tests-ran
        with:
          path: ./coverage-results
          name: The specs job

      - name: Upload coverage results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-results-specs
          path: ./coverage-results/
          retention-days: 1

  integration:
    needs: [dotnet-build, dotnet-build-development]
    permissions:
      contents: read
      packages: read
    uses: ./.github/workflows/integration.yml
    secrets: inherit
    with:
      # Integration shards restore the DEVELOPMENT-symbol build and run --no-build;
      # the Docker image still comes from dotnet-build.
      dotnet-cache-key: ${{ needs.dotnet-build-development.outputs.dotnet-cache-key }}
      chronicle-image: ${{ needs.dotnet-build.outputs.chronicle-image }}
      # Every storage backend on the nightly schedule and on manual dispatch;
      # pull requests run the pull request set (mongodb + sqlite) the generator
      # declares in PULL_REQUEST_DATABASES.
      all-providers: ${{ github.event_name != 'pull_request' }}

  integration-api:
    runs-on: ubuntu-latest
    needs: [dotnet-build]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .Net
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            ${{ env.DOTNET8_VERSION }}
            ${{ env.DOTNET9_VERSION }}
            ${{ env.DOTNET_VERSION }}

      - uses: actions/cache@v4
        id: dotnet-x64-output
        with:
          path: ./**/bin
          key: ${{ env.DOTNET_CACHE }}
          # The next step copies straight out of the restored bin directory, so this job cannot survive a miss.
          # Left at the default, a miss - including a transient "operation cannot be completed in timeout" on
          # restore - carried on to fail at `cp: cannot stat`, which reads as a broken build rather than as the
          # cache problem it is. Fail here instead, where the message names the cause and a re-run fixes it.
          # The equivalent restores in integration.yml are deliberately left tolerant: those jobs build as part
          # of `dotnet test`, so a miss there costs time rather than correctness.
          fail-on-cache-miss: true

      - name: Login to GitHub Docker Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker image from cached binaries
        run: |
          rm -rf Source/Kernel/Server/out
          mkdir -p Source/Kernel/Server/out
          cp -r Source/Kernel/Server/bin/Release/net10.0/. Source/Kernel/Server/out/
          docker build -t "${{ env.CRATIS_CHRONICLE_LOCAL_IMAGE }}" -f Docker/Local/Dockerfile .

      - name: Run API integration tests with coverage
        working-directory: ./Integration/Api
        # Keep build enabled for .NET 10 test projects. `--no-build` causes an
        # invalid test assembly argument failure in this workflow context.
        run: dotnet test --logger "console;verbosity=normal" --configuration Release -p:DisableDockerBuild=true --collect:"XPlat Code Coverage" --results-directory ./coverage-results --logger "trx;LogFileName=results.trx"

      - name: Assert API integration tests ran
        uses: ./.github/actions/assert-tests-ran
        with:
          path: ./Integration/Api/coverage-results
          name: The API integration job

      - name: Upload coverage results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-results-integration-api
          path: ./Integration/Api/coverage-results/
          retention-days: 1

  mongodb:
    runs-on: ubuntu-latest
    needs: [dotnet-build]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .Net
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            ${{ env.DOTNET8_VERSION }}
            ${{ env.DOTNET9_VERSION }}
            ${{ env.DOTNET_VERSION }}

      - uses: actions/cache@v4
        id: dotnet-x64-output
        with:
          path: ./**/bin
          key: ${{ env.DOTNET_CACHE }}

      - name: Login to GitHub Docker Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Run MongoDB integration tests with coverage
        working-directory: ./Integration/MongoDB
        run: dotnet test --no-build --logger "console;verbosity=normal" --configuration Release --collect:"XPlat Code Coverage" --results-directory ./coverage-results --logger "trx;LogFileName=results.trx"

      - name: Assert MongoDB integration tests ran
        uses: ./.github/actions/assert-tests-ran
        with:
          path: ./Integration/MongoDB/coverage-results
          name: The MongoDB integration job

      - name: Upload coverage results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-results-mongodb
          path: ./Integration/MongoDB/coverage-results/
          retention-days: 1

  coverage-merge-and-cache:
    if: github.event_name != 'pull_request_target'
    runs-on: ubuntu-latest
    needs: [specs, integration, integration-api, mongodb]
    outputs:
      coverage-cache-key: ${{ steps.export-coverage-key.outputs.key }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.sha }}

      - name: Setup .Net
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Download all coverage artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./all-coverage-results
          # Download all coverage artifacts using wildcard pattern
          pattern: coverage-results-*

      - name: Merge coverage results
        run: |
          # Create directories for merged coverage
          mkdir -p ./coverage-results
          mkdir -p ./coverage-reports
          
          # Find all coverage.cobertura.xml files from artifacts and copy them
          find ./all-coverage-results -name "coverage.cobertura.xml" -type f | while read file; do
            echo "Found coverage file: $file"
            # Create unique directory for each coverage file
            dirname_hash=$(echo "$file" | md5sum | cut -d' ' -f1)
            mkdir -p "./coverage-results/$dirname_hash"
            cp "$file" "./coverage-results/$dirname_hash/coverage.cobertura.xml"
          done
          
          echo "Merged coverage results:"
          find ./coverage-results -name "coverage.cobertura.xml" | head -20

      - name: Install coverage tools
        run: dotnet tool install --global dotnet-reportgenerator-globaltool

      - name: Generate merged coverage report
        run: |
          reportgenerator \
            -reports:"./coverage-results/**/coverage.cobertura.xml" \
            -targetdir:"./coverage-reports" \
            -reporttypes:"Cobertura;JsonSummary" \
            -assemblyfilters:"-*.Specs;-xunit*;-NSubstitute*"
          
          echo "Generated coverage report:"
          ls -la ./coverage-reports/

      - name: Cache merged coverage
        uses: actions/cache@v4
        with:
          path: ./coverage-reports
          key: coverage-cache-${{ github.sha }}

      - name: Export coverage cache key
        id: export-coverage-key
        run: |
          echo "key=coverage-cache-${{ github.sha }}" >> "$GITHUB_OUTPUT"

      - name: Collect coverage statistics
        run: |
          .github/scripts/collect-coverage.sh \
            "./coverage-reports" \
            "./Documentation/statistics/coverage-data.js" \
            "${{ github.head_ref || github.ref_name }}" \
            "${{ github.sha }}" \
            "${{ github.event.head_commit.message || format('Coverage update for {0}', github.head_ref || github.ref_name) }}"

  # Running the benchmarks takes ~13 minutes and sits on the critical path behind dotnet-build, which made it
  # the single thing deciding how long a pull request takes to go green. It cannot fail a build either
  # (fail-on-alert is false), so the series is tracked on main by benchmarks.yml instead.
  #
  # What still has to hold per pull request is that the benchmark projects BUILD, or a broken one lands green
  # and breaks main. Building them the way BenchmarkDotNet does - it regenerates and rebuilds with
  # ArtifactsPath set, which changes which project references apply - is what catches that, and costs ~2
  # minutes off the critical path rather than ~13 on it.
  benchmarks-build:
    runs-on: ubuntu-latest
    needs: [dotnet-build]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .Net
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - uses: actions/cache@v4
        id: dotnet-x64-output
        with:
          path: ./**/bin
          key: ${{ env.DOTNET_CACHE }}

      - name: Build benchmarks the way BenchmarkDotNet does
        run: |
          dotnet build Benchmarks/Chronicle.Benchmarks/Chronicle.Benchmarks.csproj \
            -c Release /p:TreatWarningsAsErrors=false /p:DisableDockerBuild=true \
            /p:ArtifactsPath="${RUNNER_TEMP}/benchmark-artifacts"
          dotnet build Benchmarks/Chronicle.Benchmarks.Clustering/Chronicle.Benchmarks.Clustering.csproj \
            -c Release /p:TreatWarningsAsErrors=false /p:DisableDockerBuild=true \
            /p:ArtifactsPath="${RUNNER_TEMP}/benchmark-artifacts-clustering"
