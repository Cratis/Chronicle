<Project Sdk="Microsoft.NET.Sdk.Web">
    <PropertyGroup>
        <AssemblyName>Cratis.Chronicle.Integration.Api</AssemblyName>
        <RootNamespace>Cratis.Chronicle.Integration.Api</RootNamespace>
        <IsTestProject>true</IsTestProject>
        <GenerateProgramFile>false</GenerateProgramFile>
        <LocalImageName>$(CRATIS_CHRONICLE_LOCAL_IMAGE)</LocalImageName>
        <LocalImageName Condition="'$(LocalImageName)' == ''">cratis/chronicle:local-development</LocalImageName>
        <!-- Explicitly set TargetFramework to net10.0 to override Directory.Build.props -->
        <TargetFramework>net10.0</TargetFramework>
        <TargetFrameworks></TargetFrameworks>
        <!-- .NET 10 makes the compiler-generated Program class public, so the local Program.cs
             intentionally shadows the one from the referenced Api project (CS0436).
             The test SDK also generates an entry point that conflicts with top-level statements (CS7022). -->
        <NoWarn>$(NoWarn);CS0436;CS7022</NoWarn>
        <GenerateProgramFile>false</GenerateProgramFile>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="../../Source/Kernel/Server/Server.csproj" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Cratis.Arc" />
        <PackageReference Include="Cratis.Arc.MongoDB" />
        <PackageReference Include="Cratis.Arc.Swagger" />
        <PackageReference Include="Microsoft.AspNetCore.Mvc.Testing" />
    </ItemGroup>

    <!-- When building on macOS the publish must target linux-arm64 so the Docker image gets a
         valid Linux binary. On Linux the default RID is already linux-arm64 or linux-x64. -->
    <PropertyGroup Condition="'$(DisableDockerBuild)' != 'true'">
        <_IsOSX>$([System.Runtime.InteropServices.RuntimeInformation]::IsOSPlatform($([System.Runtime.InteropServices.OSPlatform]::OSX)))</_IsOSX>
        <_DockerPublishRid Condition="'$(_IsOSX)' == 'True' And '$([System.Runtime.InteropServices.RuntimeInformation]::ProcessArchitecture)' == 'Arm64'">linux-arm64</_DockerPublishRid>
        <_DockerPublishRid Condition="'$(_IsOSX)' == 'True' And '$([System.Runtime.InteropServices.RuntimeInformation]::ProcessArchitecture)' == 'X64'">linux-amd64</_DockerPublishRid>
        <_DockerRidArg Condition="'$(_DockerPublishRid)' != ''">-r $(_DockerPublishRid) --self-contained false</_DockerRidArg>
    </PropertyGroup>

    <Target Name="BuildDocker" AfterTargets="Build" Condition="'$(DisableDockerBuild)' != 'true'">
        <!-- Publishes through Integration/LocalKernelPublish.proj rather than a local Exec. Integration/Client
             needs the identical publish, and a solution build runs both projects in parallel; two independent
             `dotnet publish` invocations wiped and wrote the same Server/out directory at the same time, and
             each nested build of Contracts re-ran the proto generator - which deletes Source/Kernel/Protobuf
             before writing it - so the two generator runs tore each other's output down. Calling the shared
             target with the same global properties makes MSBuild's build-result cache run the publish once per
             build, whichever project reaches it first; the other caller reuses that result instead of racing it. -->
        <MSBuild Projects="$(MSBuildThisFileDirectory)../LocalKernelPublish.proj" Targets="PublishKernel" Properties="KernelPublishRid=$(_DockerPublishRid)" />
        <Exec Command="docker build -t $(LocalImageName) -f Docker/Local/Dockerfile ." WorkingDirectory="$(MSBuildThisFileDirectory)../../"/>
    </Target>

    <ItemGroup>
        <!-- Direct pin: Cratis.Arc.MongoDB floats Castle.Core to 5.2.1, whose proxy mapping regression breaks NSubstitute class proxies. -->
        <PackageReference Include="Castle.Core" />
    </ItemGroup>
</Project>
